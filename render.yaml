services:
  - type: worker
    name: delta-algo-bot
    env: python
    plan: free
    buildCommand: "pip install --upgrade pip && pip install -r requirements.txt"
    startCommand: "python -u app.py"
    autoDeploy: true